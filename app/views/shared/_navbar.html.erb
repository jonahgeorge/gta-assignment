<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "GtaAssignment", root_path, class: "navbar-brand" %>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <% if current_user.is_instructor %>
            <li><%= link_to "Sections", sections_path %></li>
          <% end %>
          <% if current_user.is_student %>
            <li><%= link_to "Section Preferences", section_preferences_path %></li>
            <li><%= link_to "Experience", experiences_path %></li>
          <% end %>
          <% if current_user.is_administrator %>
            <li><%= link_to "Assignment", administrator_assignment_path %></li>
            <li><%= link_to "Settings", administrator_settings_path %></li>
          <% end %>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Sign In", user_omniauth_authorize_path(:google_oauth2) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
