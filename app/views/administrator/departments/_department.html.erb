<% cache department do %>
  <tr>
    <td><%= link_to department.subject_code, administrator_department_path(department) %></td>
    <td><%= link_to department.name, administrator_department_path(department) %></td>
  </tr>
<% end %>
